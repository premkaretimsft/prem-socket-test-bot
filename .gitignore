# Dependencies / build output
node_modules/
lib/
dist/
*.log

# Secrets & local env -- NEVER commit (templates .env.example / env/.env.local.sample ARE tracked)
.env
.env.local
.env.*.local
.localConfigs
env/.env.local
env/.env.*.user

# Local Redis lockfile / pid (if ever placed in-repo)
*.pid

# OS / editor cruft
.DS_Store
Thumbs.db
